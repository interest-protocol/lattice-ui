'use client';

import type { FC } from 'react';

import { Layout } from '@/components';

import SwapContent from './swap-content';

const Swap: FC = () => (
  <Layout>
    <SwapContent />
  </Layout>
);

export default Swap;
